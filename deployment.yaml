apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-backend-java
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo
  template:
    metadata:
      labels:
        app: todo
    spec:
      containers:
      - name: backend
        image: tunkcalb/todo-backend-java
